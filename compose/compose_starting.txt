

1.
Uruchomienie z nazwą projektu
docker compose -p transfer_one -f ../docker-compose.yml -p 8082:8082 up

z nazwą projektu jak poniże nie diała docker compose ps i docker compose ps -a
docker compose -p "transfer_proj" -f docker-compose.yml up

2.
główne używane uruchomienie:

docker compose -f docker-compose.yml up

docker compose -f docker-compose.yml up -d

docker network create transfer_devel

-d
Detached mode. Run container in the background, print new container name.

--entrypoint CMD
Override the entrypoint of the image with CMD.

3.
LOGI


docker logs --follow compose-transfer_one-1

docker logs --follow compose-transfer_two-1

docker logs --follow compose-transfer_three-1

5.
 Pokazuje porty:

 netstat -lp